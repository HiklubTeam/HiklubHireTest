/** ToDo:
 *
 * Crea un componente usando las preguntas dadas, cada pregunta tiene 4 posibles respuestas (1 a 4).
 * El usuario puede abandonar el quiz en cualquier momento y siempre que quiera regresar debe retormar en la última pregunta donde abandonó.
 * El usuario no puede retroceder sobre preguntas ya respondidas.
 * El cuestionario debe estar optimizado para SEO.
 *
 * En App.js tienes comentado una primera aproximación de Google Analytics sin tracking ID.
 *
 * Puedes utilizar el archivo questions o crear tu propia forma de implementarlas
 *
 * El quiz se almacena en un array de 15 posiciones
 *
 * El array se pasa al componente Radar para pintar el gráfico correspondiente
 *
 */
import React from "react";
import Questions from "./Questions";

export default function Quiz() {
  return <div>Quiz</div>;
}
